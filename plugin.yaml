name: ollama-cloud-usage
version: 1.1.0
description: "Show Ollama Cloud usage (monthly usage-credit pool, per-model request counts) in the Hermes desktop app — statusbar chip + popover. Reads the official https://ollama.com/api/usage endpoint; no cookie scraping."
author: mpartipilo
license: MIT
